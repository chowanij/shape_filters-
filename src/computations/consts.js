// Let's define some consts to avoid 'magic strings'

const SQUARE = 'square';
const CIRCLE = 'circle';
const RECTANGLE = 'rectangle';

const ALLOWED_SHAPES = [SQARE, CIRCLE, RECTANGLE];

module.exports = {
    SQUARE,
    CIRCLE,
    RECTANGLE,
    ALLOWED_SHAPES
};